import ContentStudioPage from '@/pages/app/ContentStudioPage';
import { AI_FACEBOOK_PAGES_PRODUCT } from '@/lib/studio/products';

/** AI Facebook Pages — Facebook destination product of the shared Content Studio. */
export default function AIFacebookPagesPage() {
	return <ContentStudioPage product={AI_FACEBOOK_PAGES_PRODUCT} />;
}
