import BrandKitPage from '@/pages/app/BrandKitPage';
import { AI_FACEBOOK_PAGES_PRODUCT } from '@/lib/studio/products';

/** AI Facebook Pages — brand kit (shared page, product-scoped routes). */
export default function AIFacebookPagesBrandKitPage() {
	return <BrandKitPage product={AI_FACEBOOK_PAGES_PRODUCT} />;
}
